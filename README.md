# Cancer - 模块化应用框架

基于微前端（qiankun）和微服务架构的模块化应用框架。该框架允许将前端和后端功能打包成统一的"业务能力模块"，并通过"模块市场"来动态组合这些模块生成新的应用程序。

## 🚀 一键启动

```bash
# 克隆项目
git clone https://github.com/zhaosai3/Cancer.git
cd Cancer

# 使用 Docker Compose 一键启动所有服务
docker-compose up
```

启动成功后访问：http://localhost:8080

## 项目特性

- 🚀 **微前端架构**：基于 qiankun 的主应用 + 子应用架构
- 🔌 **微服务后端**：独立的业务服务，通过 API 网关统一管理
- 📦 **模块化设计**：前后端一体化的业务能力模块
- 🛒 **模块市场**：动态发现、浏览和组合业务模块
- 🔄 **动态路由**：API 网关根据模块配置自动生成路由规则

## 项目结构

```
Cancer/
├── main-app/              # Qiankun 主应用（前端容器）
│   ├── package.json
│   ├── public/
│   │   └── index.html
│   └── src/
│       └── main.js
├── api-gateway/           # API 网关
│   ├── package.json
│   └── index.js
├── modules/               # 业务模块目录
│   ├── module-market/     # 模块市场（核心服务）
│   │   ├── module.json
│   │   ├── backend/
│   │   │   ├── package.json
│   │   │   └── server.js
│   │   └── frontend/
│   │       ├── package.json
│   │       └── src/
│   │           └── main.js
│   └── module-user/       # 用户管理模块（示例）
│       ├── module.json
│       ├── backend/
│       │   ├── package.json
│       │   └── server.js
│       └── frontend/
│           ├── package.json
│           └── src/
│               └── main.js
├── docker-compose.yml     # Docker 编排文件
├── .gitignore
└── README.md
```

## 快速开始

### 方式一：Docker 一键部署（推荐）

**前置要求：**
- Docker 20+
- Docker Compose 1.29+

**启动步骤：**

```bash
# 1. 克隆项目
git clone https://github.com/zhaosai3/Cancer.git
cd Cancer

# 2. 一键启动所有服务
docker-compose up

# 或者后台运行
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

启动后所有服务会自动运行，直接访问 http://localhost:8080 即可使用。

### 方式二：本地开发

**前置要求：**
- Node.js 14+
- npm 或 yarn

1. **安装依赖**

```bash
# 安装主应用依赖
cd main-app && npm install && cd ..

# 安装 API 网关依赖
cd api-gateway && npm install && cd ..

# 安装模块市场依赖
cd modules/module-market/backend && npm install && cd ../../..
cd modules/module-market/frontend && npm install && cd ../../..

# 安装用户模块依赖
cd modules/module-user/backend && npm install && cd ../../..
cd modules/module-user/frontend && npm install && cd ../../..
```

2. **启动服务**

```bash
# 启动模块市场后端（端口 3001）
cd modules/module-market/backend && npm start &

# 启动用户模块后端（端口 3002）
cd modules/module-user/backend && npm start &

# 启动 API 网关（端口 3000）
cd api-gateway && npm start &

# 启动模块市场前端（端口 8081）
cd modules/module-market/frontend && npm start &

# 启动用户模块前端（端口 8082）
cd modules/module-user/frontend && npm start &

# 启动主应用（端口 8080）
cd main-app && npm start
```

### 访问应用

- 主应用：http://localhost:8080
- 模块市场：http://localhost:8080/module-market
- 用户管理：http://localhost:8080/user
- API 网关：http://localhost:3000

## 核心概念

### 业务能力模块

每个模块都是一个独立的业务单元，包含：

- **module.json**：模块元数据（名称、版本、路由、API 配置等）
- **backend/**：后端微服务
- **frontend/**：前端子应用（qiankun 子应用）

### 模块市场

模块市场是框架的核心组件，负责：

- 扫描和注册所有可用模块
- 提供模块浏览和搜索功能
- 向 API 网关提供路由配置
- 向主应用提供子应用注册信息

### API 网关

动态 API 网关根据模块配置自动生成路由规则，将前端请求转发到相应的后端服务。

## 开发新模块

1. 在 `modules/` 目录下创建新模块目录
2. 创建 `module.json` 配置文件
3. 实现 `backend/` 后端服务
4. 实现 `frontend/` 前端子应用
5. 重启服务，模块将自动被发现和注册

## 技术栈

- **前端**：Vue 3, qiankun
- **后端**：Node.js, Express
- **网关**：http-proxy-middleware
- **容器**：Docker, Docker Compose

## 许可证

MIT
